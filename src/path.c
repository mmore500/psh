#include "path.h"

/*
 * Attepts to execute an executable file at the given path
 * @param path the path to the executable file as a string
 * @param wait if true, will wait on execution of executable
 * @return 0 on success, 1 on failure
 */
int exec_path(char *path, char **args, bool wait) {
 return 1;
}
